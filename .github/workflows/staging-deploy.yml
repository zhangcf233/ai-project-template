name: staging-deploy

on:
  push:
    branches: ["develop"]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        run: |
          IMAGE_REPO=ghcr.io/${{ github.repository_owner }}/ai-project-template
          IMAGE_TAG=${{ github.sha }}
          docker build -f docker/nginx/Dockerfile -t ${IMAGE_REPO}:${IMAGE_TAG} .
          docker push ${IMAGE_REPO}:${IMAGE_TAG}

      - name: Deploy staging
        env:
          STAGING_ENV_FILE: ${{ secrets.STAGING_ENV_FILE }}
        run: |
          mkdir -p .runtime
          echo "$STAGING_ENV_FILE" > .runtime/staging.env
          echo "IMAGE_REPO=ghcr.io/${{ github.repository_owner }}/ai-project-template" >> .runtime/staging.env
          echo "IMAGE_TAG=${{ github.sha }}" >> .runtime/staging.env
          bash deploy/deploy.sh staging .runtime/staging.env docker-compose.staging.yml
